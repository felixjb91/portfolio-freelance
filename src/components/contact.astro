<section id="contact" class="w-full py-12 border-t border-[#ffffff10]">
  <div class="max-w-5xl mx-auto">
    <h2 class="text-lg text-[var(--sec)] mb-2 shiny-sec">Discutons</h2>
    <h3 class="text-4xl md:text-5xl font-medium text-[var(--white)] mb-6">
      Contact
    </h3>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      <div class="text-[var(--white-icon)]">
        <p class="mb-4">
            Une question ou un projet en tête ? Contactez moi.
        </p>
        <div class="flex items-center gap-2">
          <span>Localisation:</span>
          <span class="text-[var(--white)]">France, Paris</span>
        </div>
      </div>

      <div>
        <form
          id="contact-form"
          class="flex flex-col gap-4"
        >
          <input
            type="text"
            name="name"
            placeholder="Nom"
            class="px-4 py-2 bg-[var(--background-card)] text-[var(--white)] border border-[var(--white-icon-tr)] rounded-lg focus:outline-none focus:ring-2 focus:ring-[var(--sec)]"
          />
          <input
            type="email"
            name="email"
            placeholder="Email"
            class="px-4 py-2 bg-[var(--background-card)] text-[var(--white)] border border-[var(--white-icon-tr)] rounded-lg focus:outline-none focus:ring-2 focus:ring-[var(--sec)]"
          />
          <input
            type="tel"
            name="phone"
            placeholder="Téléphone"
            class="px-4 py-2 bg-[var(--background-card)] text-[var(--white)] border border-[var(--white-icon-tr)] rounded-lg focus:outline-none focus:ring-2 focus:ring-[var(--sec)]"
          />
          <textarea
            name="message"
            placeholder="Message"
            rows="6"
            class="px-4 py-2 bg-[var(--background-card)] text-[var(--white)] border border-[var(--white-icon-tr)] rounded-lg focus:outline-none focus:ring-2 focus:ring-[var(--sec)] resize-none"
          ></textarea>
          <button
            type="submit"
            class="px-4 py-2 bg-[var(--white-icon-tr)] text-[var(--white)] rounded-lg opacity-60 transition-opacity border border-[var(--white-icon-tr)] hover:opacity-100 hover:bg-[var(--white-icon-tr)]"
          >
            envoyer
          </button>
        </form>
        <div
          id="form-error"
          class="hidden mt-4 text-red-400 text-sm"
        >
        </div>
        <div
          id="form-message"
          class="hidden justify-center items-center mt-4 text-[var(--white)] text-lg"
        >
          ✅ Merci pour ton message!
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import { parsePhoneNumberFromString } from '@devmehq/phone-number-validator-js';

  const form = document.getElementById("contact-form") as HTMLFormElement;
  const formMessage = document.getElementById("form-message");
  const formError = document.getElementById("form-error");

  const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

  form.addEventListener("submit", async (e) => {
    e.preventDefault();
    formError!.classList.add("hidden");
    formError!.textContent = "";

    const name = (form.elements.namedItem("name") as HTMLInputElement).value.trim();
    const email = (form.elements.namedItem("email") as HTMLInputElement).value.trim();
    const phone = (form.elements.namedItem("phone") as HTMLInputElement).value.trim();
    const message = (form.elements.namedItem("message") as HTMLTextAreaElement).value.trim();

    const errors: string[] = [];

    if (!name) {
      errors.push("Le nom est requis");
    }

    if (!email && !phone) {
      errors.push("L'email ou le téléphone est requis");
    }

    if (email && !emailRegex.test(email)) {
      errors.push("Format d'email invalide");
    }

    if (phone) {
      const phoneNumber = phone.startsWith('+')
        ? parsePhoneNumberFromString(phone)
        : parsePhoneNumberFromString(phone, 'FR');

      if (!phoneNumber) {
        errors.push("Numéro de téléphone non reconnu");
      } else if (!phoneNumber.isValid()) {
        errors.push("Numéro de téléphone invalide (vérifiez le nombre de chiffres)");
      }
    }

    if (!message) {
      errors.push("Un message est requis");
    }

    if (errors.length > 0) {
      formError!.innerHTML = errors.join("<br>");
      formError!.classList.remove("hidden");
      return;
    }

    try {
      const response = await fetch("https://n8n.feltech.fr/webhook/portfolio/contact-form", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ name, email, phone, message }),
      });
      if (response.ok) {
        form.reset();
        form.style.display = "none";
        formMessage!.classList.remove("hidden");
        formMessage!.classList.add("flex");
      } else {
        console.error("Error response:", response.status);
        formError!.textContent = "Une erreur est survenue lors de l'envoi.";
        formError!.classList.remove("hidden");
      }
    } catch (error) {
      console.error("Error:", error);
      formError!.textContent = "Une erreur est survenue lors de l'envoi.";
      formError!.classList.remove("hidden");
    }
  });
</script>
